### Goal

Remove Astro starter branding and establish the shared asset, content, style, and document boundaries without yet building complete pages.

| Task | Work | References | Files | Dependencies | Validation |
|---|---|---|---|---|---|
| `P3-01` | Copy required Officelite SVG and favicon assets byte-for-byte from reference material into the runtime application. Do not use temporary Figma URLs. | `CON-005`, `AD-008`, `SP-NFR-004` | Proposed runtime assets in §8.3 | Phase 2 | Hash/source comparison confirms matching assets; build imports resolve. |
| `P3-02` | Remove Astro starter assets and favicon references after Officelite replacements exist. | Repository cleanup; `AD-008` | `astro.svg`, `background.svg`, current favicons | `P3-01` | Repository search finds no Astro starter asset imports or Astro favicon in rendered HTML. |
| `P3-03` | Create typed content and Plan data with Basic, Pro, Ultimate, placeholder copy, labels, and provisional messages. Leave the exact launch target unset or gated until `G-001`. | `BR-001`, `BR-007`, `CR-001`–`CR-005`; `SP-CONTENT-*` | `frontend/src/content/site.ts` | Phase 2 | Type checks pass; presentation can render all three Plans from data. |
| `P3-04` | Map Figma colors, spacing, radii, typography roles, shadows, and responsive thresholds to CSS custom properties. Preserve accepted contrast values. | `CON-002`, `CON-007`; `DESIGN.md` §§8–10, 17 | `tokens.css` | Phase 2 | Token audit compares values with `FIGMA-AUDIT.md`; no unauthorized replacement colors. |
| `P3-05` | Add global reset/base styles, body backgrounds, container rules, visually hidden utility, and reduced-motion-safe defaults. | `AR-001`, `AR-008`, `AR-010`; `SP-GLOBAL-*` | `global.css` | `P3-04` | Build passes; base page has no default margins or horizontal overflow. |
| `P3-06` | Replace `Layout.astro` with title/description props, language, viewport, Officelite favicon, global CSS, and slot. Resolve font delivery only when `G-002` is approved; use the documented fallback stack meanwhile. | `AD-001`, `G-002`; `SP-GLOBAL-001`, `003` | `Layout.astro` | `P3-01`, `P3-04`, `P3-05` | HTML metadata and favicon are correct; missing approved font does not block build. |
| `P3-07` | Create `BrandLogo.astro`, `SiteHeader.astro`, and `CtaLink.astro` with semantic static/link behavior and Figma variants. | `FR-001`, `FR-002`, `FR-010`; Figma components | Proposed component files | `P3-01`, `P3-04` | Component output has correct link semantics/names and default/hover/focus styles. |
| `P3-08` | Remove `Welcome.astro` only after `index.astro` no longer imports it. | Repository migration | `Welcome.astro`, `index.astro` | At least a temporary Officelite page shell exists | `pnpm check`, `pnpm build`, and import search pass. |
| `P3-09` | Create shared static `Countdown.astro` markup with one named group, four labeled units, light/dark variants, and stable DOM hooks. Keep values static until the Phase 5 controller is integrated. | `FR-009`, `SP-A11Y-011`, `DESIGN.md` §11.7 | `Countdown.astro` | `P3-03`, `P3-04` | Accessibility tree exposes one group and four associated values; no live region is attached to tick values. |

### Phase completion evidence

- `/` renders an Officelite-branded semantic shell.
- Astro starter artwork and copy are absent.
- Shared tokens, assets, and static countdown markup are application-owned.
- Static checks and build pass.

## 13. Phase 4 — Home vertical slice

### Goal

Deliver the complete Home experience, including responsive composition and correct link destinations, before adding dynamic countdown behavior.

| Task | Work | References | Files | Dependencies | Validation |
|---|---|---|---|---|---|
| `P4-01` | Create `PricingCard.astro` with neutral/featured variants, typed Plan input, feature list, and matching query-string CTA. | `FR-001`, `FR-003`, `SP-HOME-002`–`004`, `AD-003` | `PricingCard.astro`, `site.ts` | Phase 3 | Basic/Pro/Ultimate render in order; links contain correct lower-case Plan slugs. |
| `P4-02` | Rebuild `index.astro` with semantic header, hero, decorative illustration, generic CTA, pricing, dark launch region, countdown, and final CTA. | `FR-001`, `FR-002`, `SP-HOME-001`, `SP-NAV-001`–`007` | `index.astro`, shared components | `P4-01`, `P3-09` | Content/order matches Home specification; all actions are keyboard-focusable anchors with correct `href` values. End-to-end destination behavior closes in Phase 6 when the route exists. |
| `P4-03` | Implement compact Home composition first, including 16 px insets, centered hero, stacked cards, dark-region overlap, and countdown/CTA alignment. | `RR-001`–`RR-005`, `SP-RWD-003`, `009`, `011` | `home.css`, component styles | `P4-02` | Compare at 375 px and below 24rem; no primary-flow horizontal scrolling. |
| `P4-04` | Add medium Home transformation at `48rem`: side-by-side hero, horizontal card anatomy, centered launch region. | `SP-RWD-004`, Figma `2141:1724` | `home.css`, `PricingCard.astro` styles | `P4-03` | Compare at 768 px and immediately around 48rem. |
| `P4-05` | Add large Home transformation at `80rem`: 1110 px maximum region, hero columns, three-card row, left countdown/right CTA. | `SP-RWD-005`, Figma `2141:1599` | `home.css` | `P4-04` | Compare at 1440 px and immediately around 80rem. |
| `P4-06` | Implement CTA default, hover, and component-family focus variants without requiring active/disabled states. | `AR-003`, `RR-006`, `DESIGN.md` §§11.2–11.4, 12.2–12.3 | `CtaLink.astro` styles | `P4-02` | Pointer/keyboard state inspection at all responsive ranges. |
| `P4-07` | Add Home link-contract coverage for both generic links and all three Plan links before Sign Up exists. Assert anchor semantics and resolved `href` values without treating a temporary 404 as a successful navigation flow. | `SP-NAV-001`–`007` | `navigation.spec.ts` | `P4-02` | Browser test inspects `/sign-up/` and expected query values; full activation/navigation coverage is completed in `P6-08`. |

### Phase completion evidence

- Home implements the compact, medium, and large structural compositions using the current approved/fallback typography.
- All five Home CTAs are real links with correct destinations; full destination behavior closes when Sign Up exists.
- Pro is featured while generic navigation remains Basic by contract.
- Final typography and dynamic countdown fidelity remain explicitly gated for Phases 5 and 9.
- Static checks, Home link-contract tests, and build pass.

## 14. Phase 5 — Countdown feature

### Goal

Implement a tested countdown boundary that is independent of the page structure and ready for the future API source.

| Task | Work | References | Files | Dependencies | Validation |
|---|---|---|---|---|---|
| `P5-01` | Implement a pure function that derives whole days, remaining hours, minutes, and seconds from `target - now`; keep time injection explicit. | `FR-009`; `SP-COUNT-001`–`005` | `countdown.ts` | Phase 2 | Deterministic unit tests cover ordinary, boundary, and throttling-recovery inputs. |
| `P5-02` | Implement two-digit minimum formatting without truncating values longer than two digits. | `SP-COUNT-006` | `countdown.ts`, tests | `P5-01` | Tests cover `0`, `7`, `14`, and `100+` days. |
| `P5-03` | Implement `initCountdown.ts` to initialize all countdown roots, update immediately, schedule approximately once per second, and recalculate from wall clock. | `SP-COUNT-007`–`009`, `AR-007` | `initCountdown.ts` | `P5-01`, `P5-02` | Fake-clock/module test and browser observation show catch-up after delayed timer. |
| `P5-04` | Confirm `G-001`; add the approved placeholder timestamp to content/config and initialize the Home countdown. | `G-001`, `SP-CONTENT-003` | `site.ts`, `index.astro` | Approved target; `P5-03` | Visible date and calculated values correspond to approved target and update once per second. |
| `P5-05` | Confirm countdown changes do not produce a one-second live announcement and stop or clean up safely when the page is unloaded. | `AR-007`, `SP-A11Y-005` | `initCountdown.ts`, accessibility test | `P5-03` | Accessibility inspection; no continuously updating live region. |

### Phase completion evidence

- Pure countdown tests pass.
- Home updates once per second from an approved unambiguous target.
- Delayed browser timers recalculate correctly.
- Countdown values are not announced every second.

## 15. Phase 6 — Sign Up page and Plan context

### Goal

Deliver the complete Sign Up page structure, responsive layout, native form semantics, and Plan-context initialization before storage work.

| Task | Work | References | Files | Dependencies | Validation |
|---|---|---|---|---|---|
| `P6-01` | Create `SignUpForm.astro` with persistent programmatic labels, required native controls, exact supported Plan options, field-message hooks, a configurable form-result hook, and submit button. Add `novalidate` to the form so the controller can render the required associated messages while retaining native `required`, input types, and `ValidityState`. Do not make placeholders the sole accessible names or lock final result styling before `G-003`. | `FR-004`–`FR-006`, `SP-FORM-*`, `SP-SIGNUP-005`–`007`, `SP-VAL-002` | `SignUpForm.astro` | Phase 3 | Accessibility tree exposes names, required state, native select, and submit button; invalid submit reaches the controller rather than being intercepted by browser bubbles. |
| `P6-02` | Implement `plan.ts` with supported slugs, query parsing, unsupported/missing fallback to Basic, and mapping to the native select value. | `FR-003`, `FR-012`, `AD-003`; `SP-NAV-003`–`004` | `plan.ts`, `plan.test.ts` | `site.ts` Plan data | Unit tests cover absent, exact valid lower-case, unsupported (including mixed-case), and unrelated parameters; no unapproved repeated-parameter policy is asserted. |
| `P6-03` | Create `/sign-up/` with linked logo, introduction, static countdown markup, form, decorative side region, and page-level initializer imports. Live countdown initialization is enabled only when the Phase 5 controller and approved `G-001` target are available; the route itself does not wait for Home completion. | `FR-004`, `FR-010`, `SP-SIGNUP-001`–`004` | `pages/sign-up/index.astro` | `P6-01`, `P3-09`; `P5-03` and `G-001` only for live countdown | Route builds and contains one H1, named logo link, countdown group, and all controls. |
| `P6-04` | Initialize Plan from the URL once, then allow the user to change it through native select behavior. | `SP-SIGNUP-003`–`006`, `SP-KEY-004` | `initSignUpForm.ts`, `plan.ts` | `P6-02`, `P6-03` | Direct/generic entry selects Basic; each valid query selects matching option; user change persists. |
| `P6-05` | Implement compact Sign Up layout with 16 px main insets, 24 px form outer insets, 20 px form padding, centered content, four tiles when they fit, and wrapping when required. | `SP-RWD-006`, `011`, Figma `2141:1940` | `sign-up.css` | `P6-03` | Compare at 375 px and narrow/enlarged-text cases. |
| `P6-06` | Implement medium and large Sign Up transformations, including 445 px reference form width and large light/dark split with 1110 px region. | `SP-RWD-007`–`008`, Figma `2141:1896`, `2141:1680` | `sign-up.css` | `P6-05` | Compare at 768 px, 1321 px, and breakpoint boundaries. |
| `P6-07` | Implement text-field, native select, and submit-button default, hover, and component-family focus visuals while preserving native control semantics. | `AR-003`, `RR-006`; Figma `10:503`, `10:512`, `10:526` | `SignUpForm.astro` styles, `sign-up.css` | `P6-01` | Keyboard focus is visible and not clipped for every field and the submit action; the native select menu still opens normally. |
| `P6-08` | Complete navigation browser tests: generic and direct Basic, Plan-specific values, unsupported fallback, and Sign Up logo Home link. | `FR-002`, `FR-003`, `FR-010`, `FR-012`; `SP-NAV-*` | `navigation.spec.ts` | `P6-04` | All navigation paths pass by pointer and keyboard. |

### Phase completion evidence

- `/sign-up/` implements the three base structural compositions with the current fallback/approved typography and without claiming final feedback-state fidelity.
- Plan is a native select with exactly three supported options.
- Direct, generic, valid Plan, and invalid Plan entry behave correctly.
- Logo returns Home.
- Final feedback styling, optional supporting Plan text, and font fidelity remain gated for Phase 9.

## 16. Phase 7 — Validation and accessible feedback shell

### Goal

Prevent invalid persistence attempts and expose field-specific feedback without depending on final approved visual treatment.

| Task | Work | References | Files | Dependencies | Validation |
