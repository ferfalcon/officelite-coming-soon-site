- [ ] No form record is sent remotely.

### 25.2 Design and responsive

- [ ] Home matches compact, medium, and large design intent.
- [ ] Sign Up matches compact, medium, and large design intent.
- [ ] Layout behaves correctly below/at/above 24rem, 48rem, and 80rem.
- [ ] Default, hover, and focus states match supplied component-family intent.
- [ ] Long content and feedback grow without clipping or overlap.
- [ ] No primary-flow horizontal scrolling occurs at 375 px or tested intermediate widths.
- [ ] Decorative assets are reused and remain nonessential.
- [ ] Native select platform variance and accepted contrast deviation are documented.

### 25.3 Accessibility

- [ ] Pages use semantic landmarks and one H1 each.
- [ ] All interactive controls have correct native roles and accessible names.
- [ ] Every form field has a persistent programmatic label.
- [ ] Required, invalid, and error relationships are exposed.
- [ ] Keyboard-only users can complete navigation, selection, validation, submission, and retry.
- [ ] Focus is visible and not clipped at all ranges.
- [ ] Success and storage failure are announced once without unexpected focus movement.
- [ ] Countdown ticks are not announced every second.
- [ ] Decorative assets are hidden from assistive technology.
- [ ] Reflow, zoom, text enlargement, and reduced motion preserve primary functionality.
- [ ] No full WCAG AA claim is made while accepted contrast failures remain.

### 25.4 Quality

- [ ] Clean lockfile installation succeeds from `frontend/`.
- [ ] `pnpm check` succeeds.
- [ ] Unit and IndexedDB integration tests succeed.
- [ ] `pnpm build` succeeds and creates static output.
- [ ] Critical Playwright flows succeed in isolated browser contexts with deterministic IndexedDB cleanup.
- [ ] Automated accessibility checks and manual checklist are complete.
- [ ] Visual comparison evidence covers all six Figma frames and interaction examples.
- [ ] Task files, test descriptions/coverage records, and review evidence reference applicable requirement/specification IDs; production source comments are added only where they clarify a non-obvious contract.

### 25.5 Deployment and documentation

- [ ] Vercel root, build, and output settings are verified.
- [ ] Preview direct-loads `/` and `/sign-up/` successfully.
- [ ] Production personal-data disposition is approved or production release is explicitly blocked/demo-only.
- [ ] `README.md` and `frontend/README.md` contain real commands/current architecture or the generated frontend README has been intentionally removed.
- [ ] Upstream documents are updated only for approved decisions or actual deviations.
- [ ] Final validation evidence, accepted risks, deployment identifier, rollback steps, and IndexedDB recovery constraints are recorded.

## 26. Requirement and specification traceability

| Plan area | Requirements | Specifications / architecture |
|---|---|---|
| Phases 3–4 — shell and Home | `FR-001`, `FR-002`, `FR-003`, `AR-001`, `AR-005`, `RR-001`–`RR-006`, `CON-005` | `SP-GLOBAL-*`, `SP-HOME-*`, `SP-NAV-*`, `AD-001`, `AD-008` |
| Phase 5 — countdown | `FR-009`, `AR-007`, `CR-003` | `SP-COUNT-*`, `SP-A11Y-005`, `AD-002` |
| Phase 6 — Sign Up/Plan | `FR-004`, `FR-005`, `FR-010`, `FR-012`, `BR-001`, `BR-002`, `CON-004` | `SP-SIGNUP-*`, `SP-FORM-001`–`004`, `SP-NAV-*`, `AD-003` |
| Phase 7 — validation | `FR-006`, `AR-004`, `DR-002` | `SP-VAL-*`, `SP-FORM-005`, `SP-A11Y-004` |
| Phase 8 — persistence/results | `FR-007`, `FR-008`, `FR-011`, `BR-004`, `DR-001`–`DR-007` | `SP-SUBMIT-*`, `SP-DATA-*`, `SP-EDGE-004`, `AD-005`, `AD-006` |
| Phase 9 — responsive/accessibility | `AR-001`–`AR-010`, `NFR-002`, `CON-007` | `SP-RWD-*`, `SP-KEY-*`, `SP-A11Y-*`, `SP-EDGE-*`, `AD-007` |
| Phases 2 and 10 — quality/release | `NFR-001`–`NFR-004`, `CON-002`, `CON-006` | `SP-NFR-*`, `AD-009`, `AD-010` |

## 27. Review pass 1 — Completeness and correctness

Completed after Stage 8 corrections:

- Rechecked the executable `frontend/` repository, including the generated `frontend/README.md`, package scripts, Astro configuration, strict TypeScript configuration, starter page, and asset references.
- Rechecked Home, Sign Up, and interaction-state Figma evidence.
- Challenged every phase for incorrect repository assumptions, hidden dependencies, task size, ordering, missing integration, states, migration, accessibility, privacy, deployment, and recovery work.
- Renumbered phases and task identifiers to align directly with the Stage 9 `Phase-01--Task-01.md` convention.
- Removed false serialization between Home and Sign Up and distinguished static countdown markup from approved live-target integration.
- Added the native form-validation strategy required to combine native semantics with controlled field messages.
- Added missing submit-button state work, persistence-after-reload verification, browser-test isolation, stale frontend-documentation cleanup, and explicit rollback rules.
- Split the broad visual-review task into separate Home, Sign Up, and interaction-state evidence.

## 28. Review pass 2 — Consistency, traceability, and uncertainty

Completed after applying the corrections:

- Verified all task IDs are unique and use phases `P1` through `P10`.
- Verified the phase overview, section headings, critical path, dependencies, and traceability table use the same numbering.
- Verified Home link-contract tests do not claim successful route navigation before Sign Up exists.
- Verified Sign Up may proceed after shared foundations without waiting for Home completion.
- Verified native email validity is sourced from `ValidityState`, not a custom regular expression.
- Verified final visual claims remain gated by font, feedback, and optional Plan-supporting-text decisions.
- Verified persistence tests cover transaction completion, failure/retry, reload durability, and test isolation.
- Verified no task adds a production API, remote record transmission, production-only test hooks, an IndexedDB uniqueness rule, or silent data deletion.
- Verified release work covers both README files, nested working directories, browser setup, privacy restrictions, deployment verification, and code/data rollback implications.
- Verified remaining stakeholder decisions are visible as gates and do not prevent creation of implementation task files.

## 29. Stage completion

- **Files modified:** `PLAN.md`
- **Review record:** `PLAN-REVIEW.md`
- **Important refinements:** Phase/task renumbering, parallel Home/Sign Up ordering, controlled native validation, complete submit states, reload persistence, isolated E2E storage, split visual reviews, dual-README cleanup, and concrete rollback/recovery rules.
- **Assumptions retained:** Exact filenames, `data-*` DOM hooks, one typed content module, and the proposed development-only test stack. Compatible package versions remain selected during implementation rather than invented in documentation.
- **Open gates:** Placeholder timestamp, font delivery, feedback design/copy, post-success behavior, sequential duplicates, retention/privacy, browser support, optional Plan secondary text, Vercel settings, and CI inclusion.
- **Blocking status:** No unresolved technical decision blocks task decomposition. Individual implementation tasks remain gated where their observable behavior depends on stakeholder approval.
- **Readiness:** **Ready with documented risks.**
