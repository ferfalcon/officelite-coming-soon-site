name: Design workflow remote commands

on:
  issues:
    types:
      - opened

permissions: {}

jobs:
  execute:
    if: github.event.issue.title == '[design-workflow] command'
    permissions:
      contents: write
      issues: write
    uses: ferfalcon/figma-to-implementation-workflow/.github/workflows/design-workflow-command.yml@7d63188242a09656fe02f621606712b42fbe6b4d
